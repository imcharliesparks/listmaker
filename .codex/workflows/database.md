# Database Workflow

Prisma generation
- `bun db:generate` - generate Prisma client.

Schema changes
- `bun db:push` - push schema to database (no migrations).

Seed data
- `bun db:seed` - seed the database.

Studio
- `bun db:studio` - run Prisma Studio.

Notes
- Run all commands from repo root.
